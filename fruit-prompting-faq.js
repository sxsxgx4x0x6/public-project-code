// FAQ / Reference Popup Widget — generated by faq-widget-generator skill
(function() {
    'use strict';
  
    // ============================================
    // CONFIGURATION
    // ============================================
    const CONFIG = {
      zIndex: 10000,
      shineEnabled: true,
      shineInterval: 240000,
      searchEnabled: false,
      searchDebounceMs: 150,
      renderMode: 'collapsible', // 'collapsible' or 'prose'
      multiAccentMode: false,
      title: 'Prompting FAQ',
      buttonText: 'Click Here for Help',
      footerText: 'Prompting FAQ',
      accentPrimary: '#4f6cb0',
      accentPrimaryBg: '#e8edf7',
      accentDark: '#1a2a4a',
      closeBtnBg: '#9b3c34'
    };
  
    // ============================================
    // CONTENT DATA
    // ============================================
    const CONTENT_DATA = [
    {
      "id": "failures-escape-hatch",
      "section": "Section 1 · Failures & Escape Hatch",
      "items": [
        {
          "q": "Can I submit if I only got a failure on turn 1 and not the final turn?",
          "a": "Yes. The failure just needs to happen on at least one turn in the conversation, it doesn't need to be on the final turn."
        },
        {
          "q": "After using the escape hatch, can I start a new task on the same project?",
          "a": "No, after using the escape hatch you should move to a different project and give the models time to recover. For non-technical failures (couldn't get Model B to fail), wait at least 24 hours before returning."
        },
        {
          "q": "Do I need to submit the task after I check the escape hatch box? I don't understand it.",
          "a": "Follow the directions in the escape hatch area carefully. You'll need to submit the incomplete task after checking the box. This is so that a task is populated so you can report time."
        }
      ]
    },
    {
      "id": "model-behavior",
      "section": "Section 2 · Model Behavior",
      "items": [
        {
          "q": "Model B keeps giving better responses than Model A. Is something wrong?",
          "a": "No, that's the challenge of the task. If you've tried multiple prompt variations and can't get a failure, use the escape hatch and try a different approach at a later time or switch to a different project."
        },
        {
          "q": "My prompt passed the checker as \"excellent,\" but Model B is still better. Does a checker pass guarantee a failure?",
          "a": "No. The checker evaluates whether your prompt is usable and realistic, but it doesn't guarantee a split. A passing prompt is just the minimum bar. Challenging the model is a separate problem you solve by crafting prompts with the right type of complexity, implicit constraints, or scope cues."
        },
        {
          "q": "What's the knowledge cut-off for the models? What if one model appears to have more up-to-date information?",
          "a": "Some models might make a claim that appears to cover information beyond the knowledge cut-off. This is fine. Evaluate them based on the information that's actually presented."
        },
        {
          "q": "Are you sure the models are correct? I can't get them to fail!",
          "a": "Eliciting model failures of any type can be very difficult. Models are continuously trained with this data and are constantly improving. This work may not be for you, but you're encouraged to try your best!"
        }
      ]
    },
    {
      "id": "prompts-content",
      "section": "Section 3 · Prompts & Content",
      "items": [
        {
          "q": "Does the prompt need to be fully original? Can I paste in articles or documents?",
          "a": "The prompt itself must be your original work. Reference material (eg articles, excerpts, contracts, Wikipedia passages) is fine to include alongside the prompt for summarization, extraction, or closed QA tasks."
        },
        {
          "q": "Can I use prompts from my real-life use of [insert model]?",
          "a": "Sure! As long as they meet the project's requirements, you can incorporate them or modify them to suit the project. Make sure any personally identifying information is removed or altered."
        }
      ]
    },
    {
      "id": "tools-process",
      "section": "Section 4 · Tools & Process",
      "items": [
        {
          "q": "The \"check your work\" tool disagrees with me and won't let me proceed. What do I do?",
          "a": "If you're 100% confident in your failure, check the box to override and proceed. Be specific in your rationale so the evaluator understands your reasoning. After about 3 attempts, note the disagreement in the explanation field and move on."
        },
        {
          "q": "Are there additional guides for understanding the categories and topic?",
          "a": "There are not. Interpret the categories as best as you can. They are not meant to be rigid boxes. If you are mostly sure your prompt meets the short descriptions in the task, you'll be fine!"
        }
      ]
    }
  ];
  
    // ============================================
    // STATE
    // ============================================
    let isOpen = false;
    let searchQuery = '';
    let openItemKey = null;
    let shineIntervalHandle = null;
    let searchDebounceTimer = null;
  
    // ============================================
    // ICONS
    // ============================================
    const ICONS = {
      question: '<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M8.228 9c.549-1.165 2.03-2 3.772-2 2.21 0 4 1.343 4 3 0 1.4-1.278 2.575-3.006 2.907-.542.104-.994.54-.994 1.093m0 3h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>',
      close: '<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/></svg>',
      search: '<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/></svg>',
      chevron: '<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5"><polyline points="9 18 15 12 9 6"/></svg>',
      book: '<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/></svg>'
    };
  
    // ============================================
    // STYLES
    // ============================================
    const STYLES = `
      @import url('https://fonts.googleapis.com/css2?family=DM+Sans:ital,wght@0,400;0,500;0,600;0,700;1,400&family=JetBrains+Mono:wght@400;500&display=swap');
  
      @keyframes sfqShine {
        0%   { background-position: 200% center; }
        100% { background-position: -200% center; }
      }
      @keyframes sfqFadeIn {
        from { opacity: 0; transform: translate(-50%,-50%) scale(0.96); }
        to   { opacity: 1; transform: translate(-50%,-50%) scale(1); }
      }
      @keyframes sfqPulse {
        0%, 100% { box-shadow: 0 2px 12px rgba(30,58,95,.25), 0 0 0 3px ${CONFIG.accentPrimary}4D; }
        50%      { box-shadow: 0 2px 12px rgba(30,58,95,.25), 0 0 0 5px ${CONFIG.accentPrimary}26, 0 0 24px ${CONFIG.accentPrimary}1F; }
      }
  
      /* Trigger Button — sized ~15% larger than reference */
      .sfq-trigger {
        position: fixed;
        bottom: 24px;
        left: 24px;
        z-index: ${CONFIG.zIndex};
        height: 53px;
        padding: 0 23px;
        border-radius: 14px;
        border: none;
        cursor: pointer;
        background: ${CONFIG.accentDark};
        color: #fff;
        font-family: 'DM Sans', -apple-system, sans-serif;
        font-size: 15.5px;
        font-weight: 600;
        letter-spacing: 0.2px;
        display: inline-flex;
        align-items: center;
        gap: 9px;
        box-shadow: 0 2px 12px rgba(30,58,95,.25), 0 0 0 3px ${CONFIG.accentPrimary}4D;
        transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
        ${CONFIG.shineEnabled ? 'animation: sfqPulse 2.8s ease-in-out infinite;' : ''}
        white-space: nowrap;
      }
      .sfq-trigger:hover {
        transform: translateY(-2px) scale(1.03);
        box-shadow: 0 4px 20px rgba(30,58,95,.3), 0 0 0 4px ${CONFIG.accentPrimary}66;
      }
      .sfq-trigger.sfq-open {
        animation: none;
        background: ${CONFIG.closeBtnBg};
        box-shadow: 0 2px 12px rgba(155,60,52,.3), 0 0 0 3px ${CONFIG.closeBtnBg}40;
      }
      .sfq-trigger.sfq-shining {
        background: linear-gradient(90deg, ${CONFIG.accentDark} 0%, ${CONFIG.accentPrimary} 30%, rgba(255,255,255,0.18) 50%, ${CONFIG.accentPrimary} 70%, ${CONFIG.accentDark} 100%);
        background-size: 300% auto;
        animation: sfqShine 2s ease-in-out;
      }
      .sfq-trigger svg { width: 18px; height: 18px; flex-shrink: 0; }
  
      /* Overlay */
      .sfq-overlay {
        position: fixed;
        inset: 0;
        z-index: ${CONFIG.zIndex + 1};
        background: rgba(15,20,30,0.38);
        backdrop-filter: blur(5px);
        opacity: 0;
        pointer-events: none;
        transition: opacity 0.3s ease;
      }
      .sfq-overlay.sfq-visible { opacity: 1; pointer-events: auto; }
  
      /* Panel */
      .sfq-panel {
        position: fixed;
        z-index: ${CONFIG.zIndex + 2};
        top: 50%;
        left: 50%;
        transform: translate(-50%,-50%) scale(0.96);
        width: 75vw;
        max-width: 860px;
        height: 75vh;
        max-height: 720px;
        background: #fafaf8;
        border-radius: 18px;
        box-shadow: 0 24px 80px rgba(0,0,0,0.2), 0 2px 12px rgba(0,0,0,0.08);
        display: flex;
        flex-direction: column;
        opacity: 0;
        pointer-events: none;
        transition: opacity 0.3s ease, transform 0.3s ease;
        overflow: hidden;
        border: 1px solid rgba(0,0,0,0.06);
        font-family: 'DM Sans', -apple-system, sans-serif;
      }
      .sfq-panel.sfq-visible {
        opacity: 1;
        pointer-events: auto;
        transform: translate(-50%,-50%) scale(1);
        animation: sfqFadeIn 0.3s ease forwards;
      }
  
      /* Header */
      .sfq-header {
        padding: 22px 28px 14px;
        border-bottom: 1px solid #e5e2dc;
        flex-shrink: 0;
        background: linear-gradient(180deg, #fafaf8 0%, #f4f3f0 100%);
      }
      .sfq-header-row {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: ${CONFIG.searchEnabled ? '14px' : '0'};
      }
      .sfq-header-left {
        display: flex;
        align-items: center;
        gap: 12px;
      }
      .sfq-header-icon {
        width: 38px;
        height: 38px;
        border-radius: 10px;
        background: ${CONFIG.accentDark};
        color: #fff;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
      }
      .sfq-header-icon svg { width: 18px; height: 18px; }
      .sfq-header-title {
        font-size: 16px;
        font-weight: 700;
        color: #1a1a2e;
        letter-spacing: -0.3px;
        line-height: 1.2;
      }
      .sfq-header-subtitle {
        font-size: 12.5px;
        color: #7a7870;
        margin-top: 1px;
      }
      .sfq-close-btn {
        width: 32px;
        height: 32px;
        border-radius: 8px;
        border: none;
        background: rgba(0,0,0,0.04);
        color: #64605a;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: background 0.15s, color 0.15s;
      }
      .sfq-close-btn:hover { background: rgba(0,0,0,0.08); color: #1a1a2e; }
      .sfq-close-btn svg { width: 16px; height: 16px; }
  
      /* Search */
      .sfq-search-wrap {
        position: relative;
        display: ${CONFIG.searchEnabled ? 'block' : 'none'};
      }
      .sfq-search-input {
        width: 100%;
        padding: 10px 14px 10px 38px;
        border: 1.5px solid #ddd9d1;
        border-radius: 10px;
        font-family: 'DM Sans', sans-serif;
        font-size: 14px;
        background: #fff;
        outline: none;
        color: #1a1a2e;
        transition: border-color 0.2s;
        box-sizing: border-box;
      }
      .sfq-search-input::placeholder { color: #b0aca4; }
      .sfq-search-input:focus { border-color: ${CONFIG.accentPrimary}; }
      .sfq-search-icon {
        position: absolute;
        left: 12px;
        top: 50%;
        transform: translateY(-50%);
        width: 16px;
        height: 16px;
        color: #9a9890;
        pointer-events: none;
      }
      .sfq-search-count {
        position: absolute;
        right: 12px;
        top: 50%;
        transform: translateY(-50%);
        font-size: 11.5px;
        color: #8a8680;
        background: #ebe8e3;
        padding: 2px 8px;
        border-radius: 6px;
        font-weight: 500;
      }
  
      /* Body */
      .sfq-body {
        flex: 1;
        overflow-y: auto;
        scroll-behavior: smooth;
      }
      .sfq-body::-webkit-scrollbar { width: 6px; }
      .sfq-body::-webkit-scrollbar-track { background: transparent; }
      .sfq-body::-webkit-scrollbar-thumb { background: #d4d0ca; border-radius: 3px; }
  
      /* Sections */
      .sfq-section-header {
        position: sticky;
        top: 0;
        z-index: 2;
        padding: 14px 28px 8px;
        font-size: 11px;
        font-weight: 700;
        letter-spacing: 0.9px;
        text-transform: uppercase;
        color: ${CONFIG.accentPrimary};
        background: #fafaf8;
      }
      .sfq-section-header.sfq-hidden { display: none; }
  
      /* Items */
      .sfq-item {
        border-bottom: 1px solid #eceae5;
      }
      .sfq-item.sfq-hidden { display: none; }
  
      /* Collapsible mode */
      .sfq-question-btn {
        display: flex;
        align-items: flex-start;
        gap: 10px;
        padding: 13px 28px;
        cursor: pointer;
        background: none;
        border: none;
        width: 100%;
        text-align: left;
        font-family: 'DM Sans', sans-serif;
        font-size: 14px;
        font-weight: 600;
        color: #2c2c3a;
        line-height: 1.5;
        transition: background 0.12s;
      }
      .sfq-question-btn:hover { background: ${CONFIG.accentPrimaryBg}; }
      .sfq-question-btn:focus-visible {
        outline: 2px solid ${CONFIG.accentPrimary};
        outline-offset: -2px;
        border-radius: 4px;
      }
  
      .sfq-chevron {
        flex-shrink: 0;
        width: 18px;
        height: 18px;
        margin-top: 2px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #9a9890;
        transition: transform 0.25s ease, color 0.25s ease;
      }
      .sfq-chevron svg { width: 14px; height: 14px; }
      .sfq-item.sfq-expanded .sfq-chevron {
        transform: rotate(90deg);
        color: ${CONFIG.accentPrimary};
      }
  
      .sfq-answer {
        max-height: 0;
        overflow: hidden;
        transition: max-height 0.35s ease, padding 0.25s ease;
        padding: 0 28px 0 56px;
      }
      .sfq-item.sfq-expanded .sfq-answer {
        max-height: 1200px;
        padding: 0 28px 16px 56px;
      }
      .sfq-answer-text {
        font-size: 13.5px;
        line-height: 1.7;
        color: #4a4a56;
        white-space: pre-line;
      }
  
      /* Prose mode — always-open */
      .sfq-prose-item {
        padding: 14px 28px 18px 28px;
      }
      .sfq-prose-title {
        font-size: 14.5px;
        font-weight: 600;
        color: #2c2c3a;
        margin: 0 0 8px 0;
        line-height: 1.4;
      }
      .sfq-prose-body {
        font-size: 13.5px;
        line-height: 1.7;
        color: #4a4a56;
        white-space: pre-line;
      }
  
      /* Highlight */
      .sfq-highlight {
        background: #fef3c7;
        color: #92400e;
        padding: 0.05em 0.2em;
        border-radius: 3px;
      }
  
      /* No results */
      .sfq-no-results {
        display: none;
        padding: 48px 28px;
        text-align: center;
        color: #9a9890;
        font-size: 14px;
      }
      .sfq-no-results.sfq-visible { display: block; }
      .sfq-no-results svg {
        width: 48px;
        height: 48px;
        margin-bottom: 12px;
        opacity: 0.3;
      }
  
      /* Footer */
      .sfq-footer {
        padding: 10px 28px;
        border-top: 1px solid #e5e2dc;
        background: #f4f3f0;
        flex-shrink: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
      }
      .sfq-footer-text {
        font-size: 12px;
        color: #9a9890;
      }
      .sfq-footer-text code {
        font-family: 'JetBrains Mono', monospace;
        font-size: 11px;
        background: #e5e2dc;
        padding: 1px 5px;
        border-radius: 4px;
        color: #64605a;
      }
  
      /* Responsive — also ~15% larger on mobile */
      @media (max-width: 768px) {
        .sfq-panel {
          width: 95vw;
          height: 90vh;
          max-width: none;
          max-height: none;
          border-radius: 14px;
        }
        .sfq-trigger {
          bottom: 16px;
          left: 16px;
          height: 48px;
          padding: 0 18px;
          font-size: 14.5px;
        }
      }
    `;
  
    // ============================================
    // HELPERS
    // ============================================
    function escapeHtml(text) {
      if (text == null) return '';
      return String(text).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
    }
  
    function highlightText(text, query) {
      if (!query) return escapeHtml(text);
      const escaped = escapeHtml(text);
      const regex = new RegExp(`(${query.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')})`, 'gi');
      return escaped.replace(regex, '<span class="sfq-highlight">$1</span>');
    }
  
    function debounce(fn, ms) {
      return function(...args) {
        clearTimeout(searchDebounceTimer);
        searchDebounceTimer = setTimeout(() => fn.apply(this, args), ms);
      };
    }
  
    function countTotal() {
      return CONTENT_DATA.reduce((sum, s) => sum + s.items.length, 0);
    }
  
    // ============================================
    // DOM CONSTRUCTION
    // ============================================
    function injectStyles() {
      const el = document.createElement('style');
      el.id = 'sfq-styles';
      el.textContent = STYLES;
      document.head.appendChild(el);
    }
  
    function buildDOM() {
      // Trigger
      const trigger = document.createElement('button');
      trigger.className = 'sfq-trigger';
      trigger.id = 'sfq-trigger';
      trigger.setAttribute('aria-label', 'Open ' + CONFIG.title);
      trigger.setAttribute('title', CONFIG.title);
      trigger.innerHTML = `${ICONS.question}<span>${escapeHtml(CONFIG.buttonText)}</span>`;
      document.body.appendChild(trigger);
  
      // Overlay
      const overlay = document.createElement('div');
      overlay.className = 'sfq-overlay';
      overlay.id = 'sfq-overlay';
      document.body.appendChild(overlay);
  
      // Panel
      const panel = document.createElement('div');
      panel.className = 'sfq-panel';
      panel.id = 'sfq-panel';
      panel.setAttribute('role', 'dialog');
      panel.setAttribute('aria-modal', 'true');
      panel.setAttribute('aria-label', CONFIG.title);
  
      const totalItems = countTotal();
      const itemLabel = CONFIG.renderMode === 'collapsible' ? 'questions' : 'sections';
  
      panel.innerHTML = `
        <div class="sfq-header">
          <div class="sfq-header-row">
            <div class="sfq-header-left">
              <div class="sfq-header-icon">${ICONS.book}</div>
              <div>
                <div class="sfq-header-title">${escapeHtml(CONFIG.title)}</div>
                <div class="sfq-header-subtitle">${totalItems} ${itemLabel}</div>
              </div>
            </div>
            <button class="sfq-close-btn" id="sfq-close" aria-label="Close">${ICONS.close}</button>
          </div>
          <div class="sfq-search-wrap">
            <span class="sfq-search-icon">${ICONS.search}</span>
            <input type="text" class="sfq-search-input" id="sfq-search" placeholder="Search…" autocomplete="off" />
            <span class="sfq-search-count" id="sfq-count"></span>
          </div>
        </div>
        <div class="sfq-body" id="sfq-body"></div>
        <div class="sfq-footer">
          <span class="sfq-footer-text">Press <code>Esc</code> to close · ${escapeHtml(CONFIG.footerText)}</span>
        </div>
      `;
      document.body.appendChild(panel);
    }
  
    // ============================================
    // RENDERING
    // ============================================
    function render() {
      const body = document.getElementById('sfq-body');
      const countEl = document.getElementById('sfq-count');
      const query = (searchQuery || '').toLowerCase().trim();
  
      let html = '';
      let totalVisible = 0;
  
      CONTENT_DATA.forEach((section, sIdx) => {
        let sectionHtml = '';
        let sectionVisible = 0;
        const sectionAccent = (CONFIG.multiAccentMode && section.accent) ? section.accent : CONFIG.accentPrimary;
  
        section.items.forEach((item, iIdx) => {
          const key = `${sIdx}-${iIdx}`;
          const qMatch = !query || (item.q || '').toLowerCase().includes(query);
          const aMatch = !query || (item.a || '').toLowerCase().includes(query);
          const visible = qMatch || aMatch;
          const expanded = openItemKey === key;
  
          if (visible) {
            sectionVisible++;
            totalVisible++;
  
            const qText = query ? highlightText(item.q, query) : escapeHtml(item.q);
            const aText = query ? highlightText(item.a, query) : escapeHtml(item.a);
  
            if (CONFIG.renderMode === 'prose') {
              sectionHtml += `
                <div class="sfq-item sfq-prose-item" data-key="${key}">
                  <div class="sfq-prose-title">${qText}</div>
                  <div class="sfq-prose-body">${aText}</div>
                </div>
              `;
            } else {
              const chevronStyle = expanded ? `style="color: ${sectionAccent}"` : '';
              sectionHtml += `
                <div class="sfq-item${expanded ? ' sfq-expanded' : ''}" data-key="${key}">
                  <button class="sfq-question-btn" data-key="${key}" aria-expanded="${expanded}">
                    <span class="sfq-chevron" ${chevronStyle}>${ICONS.chevron}</span>
                    <span>${qText}</span>
                  </button>
                  <div class="sfq-answer">
                    <div class="sfq-answer-text">${aText}</div>
                  </div>
                </div>
              `;
            }
          }
        });
  
        const sectionHidden = sectionVisible === 0;
        const headerStyle = CONFIG.multiAccentMode && section.accent ? `style="color: ${section.accent}"` : '';
        html += `<div class="sfq-section-header${sectionHidden ? ' sfq-hidden' : ''}" ${headerStyle}>${escapeHtml(section.section)}</div>`;
        html += sectionHtml;
      });
  
      html += `
        <div class="sfq-no-results${totalVisible === 0 ? ' sfq-visible' : ''}">
          ${ICONS.search}
          <p>No matching ${CONFIG.renderMode === 'collapsible' ? 'questions' : 'content'} found.</p>
        </div>
      `;
  
      body.innerHTML = html;
  
      if (CONFIG.searchEnabled) {
        if (query) {
          countEl.textContent = `${totalVisible} found`;
        } else {
          countEl.textContent = '';
        }
      }
  
      // Attach handlers for collapsible mode
      if (CONFIG.renderMode === 'collapsible') {
        body.querySelectorAll('.sfq-question-btn').forEach(btn => {
          btn.addEventListener('click', () => {
            const key = btn.getAttribute('data-key');
            openItemKey = openItemKey === key ? null : key;
            render();
          });
        });
      }
    }
  
    // ============================================
    // OPEN / CLOSE
    // ============================================
    function open() {
      isOpen = true;
      document.getElementById('sfq-overlay').classList.add('sfq-visible');
      document.getElementById('sfq-panel').classList.add('sfq-visible');
      const trig = document.getElementById('sfq-trigger');
      trig.classList.add('sfq-open');
      trig.querySelector('span').textContent = 'Close';
  
      searchQuery = '';
      openItemKey = null;
      if (CONFIG.searchEnabled) {
        const input = document.getElementById('sfq-search');
        input.value = '';
        setTimeout(() => input.focus(), 250);
      }
      render();
    }
  
    function close() {
      isOpen = false;
      document.getElementById('sfq-overlay').classList.remove('sfq-visible');
      document.getElementById('sfq-panel').classList.remove('sfq-visible');
      const trig = document.getElementById('sfq-trigger');
      trig.classList.remove('sfq-open');
      trig.querySelector('span').textContent = CONFIG.buttonText;
    }
  
    function toggle() {
      if (isOpen) { close(); } else { open(); }
    }
  
    // ============================================
    // SHINE EFFECT
    // ============================================
    function startShine() {
      if (!CONFIG.shineEnabled) return;
      const btn = document.getElementById('sfq-trigger');
      if (!btn) return;
      btn.addEventListener('animationend', (e) => {
        if (e.animationName === 'sfqShine') btn.classList.remove('sfq-shining');
      });
      shineIntervalHandle = setInterval(() => {
        if (!isOpen) btn.classList.add('sfq-shining');
      }, CONFIG.shineInterval);
    }
  
    // ============================================
    // INIT
    // ============================================
    function init() {
      injectStyles();
      buildDOM();
      startShine();
  
      document.getElementById('sfq-trigger').addEventListener('click', toggle);
      document.getElementById('sfq-close').addEventListener('click', close);
      document.getElementById('sfq-overlay').addEventListener('click', close);
  
      if (CONFIG.searchEnabled) {
        const debouncedSearch = debounce((val) => {
          searchQuery = val;
          openItemKey = null;
          render();
        }, CONFIG.searchDebounceMs);
        document.getElementById('sfq-search').addEventListener('input', (e) => {
          debouncedSearch(e.target.value);
        });
      }
  
      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && isOpen) close();
      });
  
      document.getElementById('sfq-panel').addEventListener('click', (e) => e.stopPropagation());
  
      render();
  
      console.log('FAQ Widget: loaded', countTotal(), 'items across', CONTENT_DATA.length, 'sections');
    }
  
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', init);
    } else {
      init();
    }
  
  })();